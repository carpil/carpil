rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // En desarrollo (emulador) todo está abierto.
    // Reemplazar con reglas reales antes de producción.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
